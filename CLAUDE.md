# Agent-Skills for Claude Code

Skills are available under `.claude/skills/`, commands under `.claude/commands/`.
Use available descriptions to select a workflow; read `cs-using` only when routing is unclear.
Follow `AGENTS.md` for project boundaries. Skill instructions contain actionable steps and
verification; examples and troubleshooting sections are optional and loaded only as needed.
