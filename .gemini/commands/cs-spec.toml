description = "Define missing requirements and boundaries in one authoritative spec / 在唯一规格中明确缺失的需求和设计边界"

prompt = '''Invoke `cs-spec-driven` for `$ARGUMENTS`. Preserve the authoritative specification and
accepted decisions. Follow the skill's scope, conditional architecture/review rules and
verification; ask only about consequential unresolved choices.

For an explicit design stress-test, use `/cs-grill-me <spec-path>`; this is optional.
'''
