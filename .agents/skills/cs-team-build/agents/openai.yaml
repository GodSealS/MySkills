interface:
  display_name: "CS Team Build"
  short_description: "Manually runs a design document through a coordinated agent team — the architect decomposes it into tasks, domain leads implement them, a reviewer."