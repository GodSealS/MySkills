interface:
  display_name: "CS Team Build"
  short_description: "Runs a design document through a coordinated agent team — the architect decomposes it into tasks, domain leads implement them, a reviewer audits."