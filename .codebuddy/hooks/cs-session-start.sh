#!/bin/bash
# Keep startup context small; skill bodies load only when selected.
printf '%s\n' '{"priority":"INFO","message":"Agent skills are available. Select by task; read cs-using only when routing is unclear."}'
